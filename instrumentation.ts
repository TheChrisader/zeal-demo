// import { initializeAdminSettings } from "./database/admin-settings/admin-settings.repository";

export async function register() {
  // Ensure this runs only on the server side during startup
  // if (process.env.NEXT_RUNTIME === "nodejs" && typeof window === "undefined") {
  //   console.log("Instrumentation hook running: Initializing Admin Settings...");
  //   await initializeAdminSettings();
  //   console.log(
  //     "Instrumentation hook: Admin Settings initialization complete.",
  //   );
  // }
  console.log("object");
}
