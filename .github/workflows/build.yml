name: Build & Deploy Docker Image to Coolify
on:
  push:
    branches:
      - main
env:
  REGISTRY: ghcr.io
  AUTHOR: thechrisader
  REPO: zeal-demo

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
      attestations: write
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Create .env File
        run: |
          echo "NODE_ENV=production" >> .env.production
          echo "NEXT_PUBLIC_APP_NAME=${{ secrets.NEXT_PUBLIC_APP_NAME }}" >> .env.production
          echo "APP_URL=${{ secrets.APP_URL }}" >> .env.production
          echo "NEXT_PUBLIC_APP_URL=${{ secrets.NEXT_PUBLIC_APP_URL }}" >> .env.production
          echo "DB_USER=${{ secrets.DB_USER }}" >> .env.production
          echo "DB_PASSWORD=${{ secrets.DB_PASSWORD }}" >> .env.production
          echo "DB_NAME=${{ secrets.DB_NAME }}" >> .env.production
          echo "DB_CLUSTER=${{ secrets.DB_CLUSTER }}" >> .env.production
          echo "DB_PORT=${{ secrets.DB_PORT }}" >> .env.production
          echo "WORLDNEWS_API_KEY=${{ secrets.WORLDNEWS_API_KEY }}" >> .env.production
          echo "WEBZ_API_KEY=${{ secrets.WEBZ_API_KEY }}" >> .env.production
          echo "EMAIL_HOST=${{ secrets.EMAIL_HOST }}" >> .env.production
          echo "EMAIL_PORT=${{ secrets.EMAIL_PORT }}" >> .env.production
          echo "EMAIL_USER=${{ secrets.EMAIL_USER }}" >> .env.production
          echo "EMAIL_PASS=${{ secrets.EMAIL_PASS }}" >> .env.production
          echo "EMAIL_FROM=${{ secrets.EMAIL_FROM }}" >> .env.production
          echo "GOOGLE_CLIENT_ID=${{ secrets.GOOGLE_CLIENT_ID }}" >> .env.production
          echo "GOOGLE_CLIENT_SECRET=${{ secrets.GOOGLE_CLIENT_SECRET }}" >> .env.production
          echo "GOOGLE_CALLBACK_URL=${{ secrets.GOOGLE_CALLBACK_URL }}" >> .env.production
          echo "NEXT_PUBLIC_GOOGLE_CLIENT_ID=${{ secrets.NEXT_PUBLIC_GOOGLE_CLIENT_ID }}" >> .env.production
          echo "BUCKET_NAME=${{ secrets.BUCKET_NAME }}" >> .env.production
          echo "BUCKET_ACCESS_KEY_ID=${{ secrets.BUCKET_ACCESS_KEY_ID }}" >> .env.production
          echo "BUCKET_SECRET_ACCESS_KEY=${{ secrets.BUCKET_SECRET_ACCESS_KEY }}" >> .env.production
          echo "BUCKET_REGION=${{ secrets.BUCKET_REGION }}" >> .env.production
          echo "BUCKET_POLICY_VERSION=${{ secrets.BUCKET_POLICY_VERSION }}" >> .env.production
          echo "CLOUDFRONT_BASE_URL=${{ secrets.CLOUDFRONT_BASE_URL }}" >> .env.production
          echo "VAPID_PUBLIC_KEY=${{ secrets.VAPID_PUBLIC_KEY }}" >> .env.production
          echo "VAPID_PRIVATE_KEY=${{ secrets.VAPID_PRIVATE_KEY }}" >> .env.production
        shell: bash

      - name: Build and push Docker image
        uses: ./.github/actions/nixpacks-build-push
        with:
          platforms: linux/amd64
          push: true
          image_name: ${{ env.REGISTRY }}/${{ env.AUTHOR }}/${{ env.REPO }}

      # - name: Deploy to Coolify
      #   run: |
      #     curl --request GET '${{ secrets.COOLIFY_WEBHOOK }}' --header 'Authorization: Bearer ${{ secrets.COOLIFY_TOKEN }}'
      #   shell: bash
